#lang curr/lib
@title{Planning Your Presentation}
@worksheet{
Translate the Racket Code into Algebra

(define x 10) @fill-in-the-blank[#:id "val1" #:label "x=10"]

(define y (* x 2)) @fill-in-the-blank[#:id "val2" #:label "y=x*2"]

(define z (+ x y)) @fill-in-the-blank[#:id "val3"]

(define age 14) @fill-in-the-blank[#:id "val4"]

(define months (* age 12)) @fill-in-the-blank[#:id "val5"]

(define days (* age 30)) @fill-in-the-blank[#:id "val6"]

(define hours (* days 24)) @fill-in-the-blank[#:id "val7"]

(define minutes (* hours 60) @fill-in-the-blank[#:id "val8"]






Name, Age and Grade: @fill-in-the-blank[#:id "Name, Age and Grade"]

Game Title: @fill-in-the-blank[#:id "Title"]

Back Story:

@free-response[#:id "Story"]

Characters:

@free-response[#:id "Characters"]

Explain a piece of your code:

@free-response[#:id "Explain Code"]
           
           }