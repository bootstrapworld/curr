#lang curr/lib
@title{Design Recipe}

A rocket is flying from Earth to Mars at 80 miles per second. Write a function that describes the distance @code{D} the rocket has traveled, as a function of time @code{t}.

@worksheet-segment["I. Contract + Purpose Statement"]

@contract-exercise{#:name  #:domain  #:range }

@worksheet-segment["II. Give Examples"]

Write two examples of your function for some sample inputs.

D(1) = @fill-in-the-blank[#:id "Example1" #:label "What should the function produce?"]

D(2) = @fill-in-the-blank[#:id "Example2" #:label "What should the function produce?"]

D(@fill-in-the-blank[#:id "Example3-1" #:label "Sample Input"]) = @fill-in-the-blank[#:id "Example3" #:label "What should the function produce?"]

@fill-in-the-blank[#:id "Example4-1" #:label "Use the function here"] = @fill-in-the-blank[#:id "Example4" #:label "What should the function produce?"]

@worksheet-segment["III. Function Header"]

Write the Function Header, giving variable names to all of you input values.

D(@fill-in-the-blank[#:id "DR-in") = @fill-in-the-blank[#:id "DR-out" ]
           
           