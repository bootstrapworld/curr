#lang curr/lib
@title{update-target}
@worksheet{
@design-recipe-exercise["double-radius" "Use the Design Recipe to write a function " @code{double-width} ", which takes in a height and a color. The function produces a solid rectangle, which is whatever height and color were passed in. Its width is twice the height."]
}
